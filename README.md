# signify

This is a project to create a sign language translator using computer vision and machine learning.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Usage

```bash
python mano.py
```
